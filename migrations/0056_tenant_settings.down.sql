-- テナント設定の受け皿を撤去する（0056 の巻き戻し）。
--
-- 表ごと落とす。解決は「テナントに行が無ければ全体へ落ちる」形なので、⚠ **この表が無くなると
-- 全テナントが全体の値で動く**（0056 を入れた直後と同じ状態）。上書きを入れたあとに巻き戻すと、
-- その上書きは消える ——巻き戻す前に、上書きしているテナントが居ないことを確かめること。
DROP TABLE IF EXISTS tenant_settings;
