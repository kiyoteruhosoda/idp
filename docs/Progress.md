# Progress

進行中・未着手タスクのみを管理する（完了したら本ファイルから削除し、必要なら `CHANGELOG.md` / `history/` へ）。

タスクは改訂後 ADR-0009（テナント独立・Entra ID 型 / UUIDv7 / 完全一致 scope / 初期 DDL 刷新）の
Phase 計画、および ADR-0010（ゼロタッチ配置・設定値の出所管理）に沿う。

## バックログ

タスクを追加するときは下の表形式で書き、優先度スコアの降順に並べる。補足が要るものだけ
表の下に「詳細」として番号付きで書く。

| 優先度 | ID | 課題内容 | 状態 | 影響度 | 重要度 | 難易度 | 工数 |
|---:|---|---|---|---:|---:|---:|---:|
| 9 | MG1 | クライアントの管理権限を管理コンソールから付与・剥奪できるようにする | ⬜未着手 | 小 | 中 | 小 | 小 |
| 6 | MG2 | 細粒度コードだけを持つ利用者を管理コンソールに入れるか決める（`whoami` の要求権限） | 🟡要判断 | 中 | 小 | 中 | 中 |

- 状態: ⬜未着手 / 🚧進行中 / 🟡要判断
- 影響度・重要度・難易度・工数: 大 / 中 / 小

### 詳細

1. **MG1**: ADR-0037 でクライアントへ管理権限を付与する API
   （`/{tenant_id}/admin/clients/{client_id}/permissions`）は入れたが、管理コンソールに画面が無い。
   現状は API を直接叩く（手順は `docs/OPERATIONS.md`「この IdP 自身を操作させたいとき」）。
   クライアント詳細画面へ、付与済みコードの一覧とチェックボックスでの付与・剥奪を足す。
2. **MG2**: `GET /admin/whoami` は `idp.tenant.admin` を要求したままなので、細粒度コードだけを
   持つ利用者は**管理 API は使えるがコンソールには入れない**（ADR-0037 の積み残し）。
   コンソールを細粒度で出し分けるなら、入口の要求権限と画面ごとの出し分け方を先に決める必要が
   ある。現状の割り切り（コンソールはテナント管理者のもの・細粒度は API 利用者向け）で足りるかを
   判断する。

## 優先度の算出

| 項目 | 小 (1) | 中 (3) | 大 (5) |
|---|---:|---:|---:|
| 影響度（修正範囲） | 単一機能・単一プロンプト | 複数機能 | システム全体・広範囲 |
| 重要度（セキュリティリスク） | なし | 社内情報への影響 | 個人情報・機密情報への影響 |
| 難易度 | 簡単 | 標準 | 難しい |

| 工数 | 補正値 |
|---|---:|
| 小 | 1 |
| 中 | 2 |
| 大 | 3 |

`優先度スコア = (影響度 × 重要度 × 難易度) ÷ 工数補正値`。同点はセキュリティリスク、
前提タスク、障害復旧性の順で先にする。

## 推奨モデルの基準

各タスクの **難易度（工数）× リスク（影響度）** で Claude モデルを割り当てる。リスクは
「テナント分離・認可境界・トークン検証・自動生成シークレット・データ基盤の整合」を重く見る。

| モデル | 割り当て基準 |
|---|---|
| **Opus 4.8** | 高リスク（セキュリティ境界・分離防御線・保証の要）または高難度（広範囲波及・設計判断を伴う） |
| **Sonnet 5** | 仕様が明確な機能実装・中程度の面。標準的な難度で判断も限定的 |
| **Haiku 4.5** | 定型・低リスク（確立パターンの反復、限定的な UI・文言・設定） |
